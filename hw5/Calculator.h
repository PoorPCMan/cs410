#ifndef CALCULATOR_H
#define CALCULATOR_H

#include <iostream>

//Declarations
char displaymenu();
int askX(int choice);
int factorial(int n);
int helppower(int x, int y);
int expTerms();
float exponential(int x, int terms);
int cosine(int x);
int nroot(int x, int n);

#endif
